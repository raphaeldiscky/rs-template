# Server
HOST=0.0.0.0
PORT=7000

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:15432/user_service

# Auth
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRATION_HOURS=24

# Logging (tracing filter syntax: info, debug, app_core=debug, etc.)
RUST_LOG=info
