# Uncomment to use mold linker for faster linking (requires: mold, clang)
# [target.x86_64-unknown-linux-gnu]
# linker = "clang"
# rustflags = ["-C", "link-arg=-fuse-ld=mold"]

# Uncomment to enable shared compilation cache (requires: sccache)
# [build]
# rustc-wrapper = "sccache"
